<% unless logged_in?(:site_admin) %>
	<%= render 'latest_post' %>
<% end %>

<div class="col-md-8 blog-main">
  <%= paginate @blogs %>
  
  <%= render @blogs %>
  
  <nav class="blog-pagination">
  	 <%= paginate @blogs %>
  </nav>
</div>
